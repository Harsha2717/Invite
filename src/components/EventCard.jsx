import { STRINGS } from "../constants/Strings";

function EventCard(params) {
    let eventName = params.eventName || STRINGS.HEART_SYMBOL;
    let eventDate = params.eventDate || "Date";
    let eventTime = params.eventTime || "Time";
    let eventVenue = params.eventVenue || "Venue";
    

}
export default EventCard;